import React,{useMemo}from'react';import EnterpriseShell from'./EnterpriseShell.jsx';import CreateWorkspace from'./CreateWorkspace.jsx';import ProfileChannelsWorkspace from'./ProfileChannelsWorkspace.jsx';import CanonicalProductionWorkspace from'./CanonicalProductionWorkspace.jsx';import CanonicalPublishWorkspace from'./CanonicalPublishWorkspace.jsx';import CanonicalMeasureWorkspace from'./CanonicalMeasureWorkspace.jsx';import{CreatorsStaged}from'./TrackBStagedSurfaces.jsx';
const S=[['remake','Create','Find an idea worth owning.'],['creators','Voices','Choose who makes it believable.'],['profiles','Channels','Choose where it should live.'],['production','Make','Turn the idea into finished work.'],['publish','Publish','Choose the moment it enters the world.'],['measurement','Learn','See what worked and use it.']];const N={remake:['creators','Choose a voice'],creators:['profiles','Choose a channel'],profiles:['production','Make it'],production:['publish','Publish it'],publish:['measurement','Learn from it'],measurement:['remake','Use the learning']};
const W=({stage})=>{switch(stage){case'remake':return <CreateWorkspace/>;case'creators':return <CreatorsStaged stage={0} onAdvance={()=>location.href='/content/profiles'}/>;case'profiles':return <ProfileChannelsWorkspace/>;case'production':return <CanonicalProductionWorkspace/>;case'publish':return <CanonicalPublishWorkspace/>;case'measurement':return <CanonicalMeasureWorkspace/>;default:return null}};
export default function ContentWorkspaceShell2(){const stage=useMemo(()=>{const p=location.pathname.match(/^\/content(?:\/([^/]+))?/ )?.[1]||'remake';return S.some(x=>x[0]===p)?p:'remake'},[]);const item=S.find(x=>x[0]===stage)||S[0],next=N[stage];return <EnterpriseShell active={stage==='remake'?'content':stage}><main className="cs-work"><style>{`.cs-work{min-height:100svh}.cs-context{height:48px;display:flex;align-items:center;justify-content:space-between;padding:0 38px;border-bottom:1px solid var(--line);color:var(--quiet);font-size:10px}.cs-context b{color:var(--ink)}.cs-context a{color:var(--ink);text-decoration:none;font-weight:800}.cs-context a i{font-style:normal;color:var(--lime);font-size:14px;margin-left:5px}.cs-hero{padding:42px 38px 34px;border-bottom:1px solid var(--line);background:radial-gradient(600px 250px at 72% 0,rgba(209,255,63,.04),transparent 70%)}.cs-over{font-size:8px;letter-spacing:.2em;text-transform:uppercase;color:var(--lime);font-weight:850}.cs-hero h1{margin:9px 0 0;max-width:12ch;font-size:clamp(50px,6vw,86px);line-height:.86}.cs-hero p{margin:14px 0 0;max-width:650px;color:var(--muted);font-size:14px;line-height:1.55}.cs-progress{display:flex;gap:18px;align-items:center;margin-top:28px;overflow:auto}.cs-step{display:flex;align-items:center;gap:7px;color:var(--quiet);font-size:9px;font-weight:700;white-space:nowrap}.cs-step span{width:23px;height:23px;display:grid;place-items:center;border:1px solid var(--line);border-radius:6px}.cs-step.active{color:var(--ink)}.cs-step.active span{background:var(--lime);border-color:var(--lime);color:#08110d}@media(max-width:650px){.cs-context{padding:0 16px}.cs-hero{padding:32px 16px 28px}.cs-hero h1{font-size:54px}.cs-progress{gap:12px}}`}</style><div className="cs-context"><span>Studio / <b>{item[1]}</b></span><a href={`/content/${next[0]}`}>Next: {next[1]}<i>→</i></a></div><header className="cs-hero"><div className="cs-over">Cornerstone studio</div><h1>{item[1]}</h1><p>{item[2]}</p><div className="cs-progress">{S.map(([id,label],i)=><div key={id} className={`cs-step${stage===id?' active':''}`}><span>0{i+1}</span>{label}</div>)}</div></header><W stage={stage}/></main></EnterpriseShell>}
