import React,{useEffect,useMemo,useState} from 'react';
import {supabase} from './supabase';

export default function CanonicalPublishWorkspace(){
 const [rows,setRows]=useState([]),[selectedId,setSelectedId]=useState(''),[date,setDate]=useState(''),[time,setTime]=useState('09:00'),[busy,setBusy]=useState(false),[message,setMessage]=useState('');
 const selected=useMemo(()=>rows.find(r=>r.id===selectedId)||null,[rows,selectedId]);
 const scheduled=rows.filter(r=>r.scheduled_at).length;const waiting=rows.filter(r=>/draft|ready|pending/i.test(String(r.status||''))).length;
 async function load(){const {data,error}=await supabase.from('track_b_publications').select('id,title,platform,status,scheduled_at,publish_attempts,last_error,created_at,updated_at,project_id,production_job_id').order('updated_at',{ascending:false}).limit(100);if(error)setMessage(error.message);else{setRows(data||[]);if(!selectedId&&data?.[0])setSelectedId(data[0].id)}}
 useEffect(()=>{load();const t=setInterval(load,8000);return()=>clearInterval(t)},[]);
 async function schedule(){if(!selected)return;if(!date){setMessage('Choose a date first.');return}setBusy(true);setMessage('Putting it on the calendar…');try{const at=new Date(`${date}T${time||'09:00'}:00`).toISOString();const {error}=await supabase.from('track_b_publications').update({status:'scheduled',scheduled_at:at}).eq('id',selected.id);if(error)throw new Error(error.message);setMessage('Scheduled.');await load()}catch(e){setMessage(e.message||'Could not schedule.')}finally{setBusy(false)}}
 return <main className="publish-desk"><style>{` .publish-desk{color:var(--text);font-family:var(--sans)}.pub-top{display:flex;justify-content:space-between;align-items:flex-end;gap:30px;padding:8px 0 30px;border-bottom:1px solid var(--border)}.pub-eyebrow{font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:var(--accent)}.pub-title{margin:8px 0 0;font-size:clamp(38px,5.4vw,68px);line-height:.98;letter-spacing:-.06em;max-width:760px}.pub-copy{margin:10px 0 0;max-width:660px;color:var(--text-muted);font-size:14px;line-height:1.6}.pub-stats{display:flex;gap:22px;padding:18px 0;color:var(--text-muted);font-size:10px}.pub-stats b{color:var(--text);font-size:18px;margin-right:5px}.pub-board{display:grid;grid-template-columns:1.15fr .85fr;border:1px solid var(--border-strong);border-radius:20px;background:var(--surface);box-shadow:var(--shadow);overflow:hidden}.pub-queue{padding:22px;background:#f0ece3;border-right:1px solid var(--border)}.pub-queue h2,.pub-launch h2{margin:5px 0 0;font-size:25px;letter-spacing:-.04em}.pub-k{font-size:9px;text-transform:uppercase;letter-spacing:.13em;font-weight:800;color:var(--text-subtle)}.pub-help{margin-top:5px;color:var(--text-muted);font-size:10px;line-height:1.5}.pub-list{display:grid;gap:7px;margin-top:17px;max-height:500px;overflow:auto}.pub-item{display:block;width:100%;text-align:left;padding:15px;border:1px solid transparent;border-radius:12px;background:transparent;color:var(--text);cursor:pointer}.pub-item:hover{background:rgba(255,255,255,.6)}.pub-item.active{background:var(--surface);border-color:var(--border-strong);box-shadow:0 5px 16px rgba(31,29,24,.06)}.pub-item strong{display:block;font-size:12px;line-height:1.35}.pub-item span{display:block;margin-top:5px;color:var(--text-subtle);font-size:9px}.pub-launch{padding:26px}.pub-selected{padding:18px;margin-top:18px;border-radius:14px;background:#1b1d1f;color:#fffdf8}.pub-selected small{display:block;color:#b9bab5;font-size:9px;text-transform:uppercase;letter-spacing:.1em}.pub-selected strong{display:block;margin-top:7px;font-size:18px;line-height:1.2}.pub-selected span{display:block;margin-top:5px;color:#c6c6bf;font-size:10px}.pub-calendar{margin-top:20px;padding-top:18px;border-top:1px solid var(--border)}.pub-label{display:block;font-size:9px;font-weight:800;letter-spacing:.11em;text-transform:uppercase;color:var(--text-subtle)}.pub-field{width:100%;margin-top:7px;padding:12px;border:1px solid var(--border-strong);border-radius:10px;background:#fffdf8;color:var(--text);font:inherit}.pub-row{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:12px}.pub-btn{width:100%;min-height:48px;margin-top:14px;border:0;border-radius:11px;background:var(--accent);color:#fffdf8;font:inherit;font-size:11px;font-weight:800;cursor:pointer}.pub-btn:disabled{opacity:.45}.pub-message{margin-top:11px;color:var(--text-muted);font-size:10px}.pub-error{margin-top:12px;padding:10px;border-radius:10px;background:rgba(180,68,68,.09);color:#9d4141;font-size:10px}.pub-after{margin-top:18px;padding-top:18px;border-top:1px solid var(--border);font-size:10px;color:var(--text-muted);line-height:1.5}.pub-after a{display:inline-block;margin-top:7px;color:var(--text);font-weight:800;text-decoration:none}.pub-empty{margin-top:18px;padding:22px;border:1px dashed var(--border-strong);border-radius:13px;color:var(--text-muted);font-size:10px;line-height:1.5}@media(max-width:800px){.pub-board{grid-template-columns:1fr}.pub-queue{border-right:0;border-bottom:1px solid var(--border)}.pub-stats{gap:14px;flex-wrap:wrap}}@media(max-width:560px){.pub-row{grid-template-columns:1fr}.pub-launch,.pub-queue{padding:17px}.pub-title{font-size:42px}} `}</style><header className="pub-top"><div><div className="pub-eyebrow">Publish</div><h1 className="pub-title">Put the right work in the right place.</h1><p className="pub-copy">Your finished pieces arrive here. Pick one, choose its moment, and send it into the market without losing the story behind it.</p></div></header><div className="pub-stats"><span><b>{rows.length}</b>pieces</span><span><b>{waiting}</b>waiting</span><span><b>{scheduled}</b>scheduled</span></div><section className="pub-board"><aside className="pub-queue"><div className="pub-k">Launch queue</div><h2>Finished work</h2><p className="pub-help">Only work that has reached publishing appears here.</p><div className="pub-list">{rows.length?rows.map(r=><button key={r.id} className={`pub-item${selectedId===r.id?' active':''}`} onClick={()=>setSelectedId(r.id)}><strong>{r.title||'Untitled piece'}</strong><span>{r.platform||'Channel'} · {r.status||'Draft'}{r.scheduled_at?` · ${new Date(r.scheduled_at).toLocaleString('en-GB')}`:''}</span></button>):<div className="pub-empty">Nothing is ready to publish yet. Finish a piece in Make first.</div>}</div></aside><section className="pub-launch"><div className="pub-k">The launch</div><h2>Choose the moment.</h2><p className="pub-help">The decision is simple: what goes live, and when.</p>{selected?<><div className="pub-selected"><small>Selected piece</small><strong>{selected.title||'Untitled piece'}</strong><span>{selected.platform||'Channel'} · {selected.status||'draft'}</span></div><div className="pub-calendar"><label className="pub-label">Date<input className="pub-field" type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><div className="pub-row"><label className="pub-label">Time<input className="pub-field" type="time" value={time} onChange={e=>setTime(e.target.value)}/></label><div><span className="pub-label">Next step</span><div className="pub-field" style={{background:'#eeeadf'}}>Record the result</div></div></div><button className="pub-btn" disabled={busy} onClick={schedule}>{busy?'Scheduling…':'Schedule it →'}</button>{selected.last_error?<div className="pub-error">{selected.last_error}</div>:null}<div className="pub-message">{message||'Nothing scheduled yet.'}</div></div><div className="pub-after">Once it is live, close the loop by recording what happened.<br/><a href="/content/measurement">Open learning →</a></div></>:<div className="pub-empty">Select a finished piece on the left to choose its launch time.</div>}</section></section></main>;
}
